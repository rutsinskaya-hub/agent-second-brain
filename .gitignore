# Environment files (ВАЖНО: секреты!)
.env
gcp-oauth.keys.json
gmail-token.json
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.Python

# Node
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Credentials / sessions (added 2026-06-10)
calendar-token.json
telethon.session

# Secrets — MCP server config (Trello/Notion/Google keys)
mcp-config.json

# Bot runtime data — mutated live by the bot, must NEVER be tracked (added 2026-06-28)
vault/daily/*.md
vault/.sessions/
vault/reminders.json
vault/topics.json
vault/attachments/*
!vault/daily/.gitkeep
!vault/attachments/.gitkeep
